# FIXME: Documentation & Implementation Inconsistencies (2026-04-12)

This file captures critical discrepancies identified during the architectural review following the Phase 11 refactor. These items must be addressed to ensure the **HOTL (Human On The Loop)** protocol and the **Conductor** workflow remain reliable.

## 1. Documentation Synchronization (High Priority)

- [x] **Update `PROCESS.md` Paths**: The document currently references `scripts/pre-submit.sh` and `scripts/bootstrap.sh`. These have moved to `core-station/bridge/`.
- [x] **Clarify Verdict Locations**: `PROCESS.md` mentions `crucible-verdicts/`, but this directory does not exist in the root. Verify if it should be created in the root or moved into `core-station/`.
- [x] **Reconcile `CLAUDE.md` Binary Paths**: `CLAUDE.md` references an `output/` directory for binaries which is missing from the filesystem.
- [x] **Standardize Workflows**: Resolve the overlap between the `conductor/workflow.md` (TDD/Git Notes) and `PROCESS.md` (Forge/Crucible Briefings). Define which protocol governs infrastructure vs. spacecraft development.

## 2. Implementation Gaps

- [x] **Track Alignment**: The `conductor/tracks/arch_review_20260409/plan.md` is currently lagging behind the implementation. Phase 11 is complete in `STATUS.md`, but the architectural review plan shows Phase 1 as unstarted.
- [x] **Missing Assembly Line Infrastructure**: `PROCESS.md` and prior Phase 7 amendments reference an `assembly-lines/` directory that is not present in the current tree.
- [x] **Analyst Inbox Location**: Determine if `analyst-inbox/` should remain in the root or be centralized under `core-station/` to match the "Meta-Factory" refactor logic.

## 3. Technical Debt & Verification

- [x] **Pre-submit Validation**: Ensure `core-station/bridge/pre-submit.sh` correctly accounts for the new `core-station/` directory depth when running component tests.
- [x] **Safety Rail Build Paths**: Verify that `CLAUDE.md` build instructions for `core-station/security` are compatible with the Rust project structure found on disk.

---
*Note: This file was generated by Gemini CLI to guide remediation of documentation drift.*
