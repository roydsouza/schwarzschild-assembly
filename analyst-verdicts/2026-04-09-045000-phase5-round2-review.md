# Analyst Verdict
**Date:** 2026-04-09 04:50:00 UTC
**Artifact:** factories/synthetic-analyst/ + root-spine/internal/grpc/server.go — Phase 5 Round 2
**Verdict:** CONDITIONAL

---

## Verdict Rationale

REGRESSION-1 is fixed. Both advisory proposals are substantive and accepted. The `worker/` and `domain-fitness/` sub-components are solid. Two mandatory anatomy items are absent, a compiled binary was committed, and a proto design gap needs a proposal.

---

## What Passes

- **REGRESSION-1 resolved:** `VetoAction` `SaveMerkleLeaf` at line 361 now has `if err != nil` → logs → returns `codes.Internal`. Identical error contract to `GATE_APPROVED`. ✓
- **`domain-fitness/metrics.go`:** All 5 metrics declared with correct IDs, thresholds, directions, and operators matching CLAUDE.md §3 Phase 5 exactly. `Collect()` returns stub simulated values — acceptable for Phase 5 scaffold. ✓
- **`worker/main.go`:** `CreateFactory` → `RegisterDomainMetrics` on startup. Correct pattern. `RegisterDomainMetrics` is called with the full declarations before the monitoring loop starts. ✓
- **`go.mod`:** Standalone module with `replace` directive pointing to `../../root-spine`. Dependency isolation is correct. ✓
- **Proposal `mcp-transport-stdio.md`:** Substantive. `StdioTransport` interface is correct. Implementation deferred to Phase 6. Accepted. ✓
- **Proposal `mcp-tool-security.md`:** The policy is exactly right — `IsSecurityAdjacent == true` → MCP `approve_action` returns error, Control Panel path required. Implementation deferred to Phase 6. Accepted. ✓

---

## Required Changes

- [ ] **MISSING-1 — `factories/synthetic-analyst/mcp-server/` absent**

  CLAUDE.md §3 Phase 5 mandates this sub-directory for all factories: "Domain-specific MCP server." This is the factory's MCP server that exposes its domain tools (DeFi data queries, macro indicator lookups, RAG retrieval) for other agents to call — distinct from the Root Spine MCP host at `:8082`. Without it, the factory cannot serve as an MCP tool provider. This is not optional: "Factory anatomy (mandatory for all factories)."

  Minimum for Phase 5 close: a `mcp-server/server.go` with at least one domain tool (e.g., `query_defi_coverage`) that returns the current stub metric value. The tool schema and handler pattern must be complete enough to serve as the reference implementation for future factories.

- [ ] **MISSING-2 — `factories/synthetic-analyst/analyst-briefing/` absent**

  CLAUDE.md §3 Phase 5 mandates "Templates for AntiGravity → Claude Code packets." Briefings filed without templates produce the inconsistencies already observed: wrong path (`root-spine/analyst-inbox/` instead of `analyst-inbox/`), missing artifact hashes, incomplete evidence. This directory closes that. Minimum: one `.md` template mirroring the required verdict-triggering format from CLAUDE.md §7.

- [ ] **PROCESS — Compiled binary committed: `factories/synthetic-analyst/worker/analyst-worker`**

  A compiled binary has no place in version control. Add `factories/synthetic-analyst/worker/analyst-worker` (or `**/analyst-worker`) to `.gitignore` at the factory level. Delete the committed binary from the index.

---

## Advisory (non-blocking for Phase 5 close)

**Metric value reporting gap:** The monitoring loop (lines 56–74) calls `collector.Collect()` every 10 seconds but has nowhere to send the values. The proto has `RegisterDomainMetrics(DomainFitnessExtension) returns (RegistrationResult)` for declaration registration, but no RPC for pushing metric snapshots. The Root Spine fitness vector is never actually updated with factory measurements.

File a proposal in `proposals/pending/` for a new `ReportMetrics` RPC (or `SubmitFitnessSnapshot`) before Phase 5 closes. The proposal should define the request type (factory ID + metric values map + timestamp), the server-side update behavior, and how OTel gauges are updated. This is proto-level work and needs a proposal per Anti-Slop Rule 8 before implementation.

The 10-second reporting interval is accepted — appropriate density for fitness vector gauges.

---

## Analyst Questions Answered

1. **10s metrics reporting interval:** Accepted. Fitness vector is a time-series gauge, not an event log. 10s is appropriate density for Phase 5. No change needed.

2. **MCP tool signature restriction:** The policy in `mcp-tool-security.md` is correct. Security-adjacent proposals must route exclusively through the Control Panel. The proposal is accepted pending implementation in Phase 6 as stated.

---

## Process Note

Briefing was filed to `root-spine/analyst-inbox/briefing.md`. The canonical path is the project-root `analyst-inbox/` directory (CLAUDE.md §2). The `analyst-briefing/` templates (MISSING-2 above) will codify this. For now: file all future briefings to `analyst-inbox/YYYY-MM-DD-HHMMSS-<topic>.md` at the project root.

---

## Fitness Vector Impact Assessment

- **Safety compliance:** RESTORED from prior session (VetoAction audit gap closed). Weight 0.30. ✓
- **Audit integrity:** RESTORED. All gate resolution paths now produce persisted Merkle leaves.
- **Dhamma alignment:** Unaffected. Pāḷi filter metric declared but stub — will improve as factory matures.
- **System performance:** Factory adds negligible load at 10s polling. Unaffected.
- **Operational cost:** Unaffected.

---

## Merkle Log Entry

```json
{
  "event_type": "GateDenied",
  "agent_id": "analyst-droid",
  "payload_hash": "<SHA-256 of this verdict file>",
  "safety_cert": "conditional — MISSING-1 mcp-server/ absent, MISSING-2 analyst-briefing/ absent, compiled binary in repo"
}
```
